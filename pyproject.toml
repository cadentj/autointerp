[project]
name = "neurondb"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torchtyping>=0.1.5",
    "triton>=3.1.0",
    "vllm==0.6.4",
]

[tool.setuptools]
packages = ["neurondb"]
